- IPA server 192.168.122.4 - needs to resolve everything

# Hypervisor / workstation: *.laptop.lab (NXDOMAIN from 192.168.1.1 is expected)
# IdM is the only place that knows laptop.lab. Home/ISP DNS will not. Options:
# - Quickest: /etc/hosts lines for 192.168.122.4/5 (idm, satellite) or test hosts.
# - On this box (NM + resolved): per-domain forward to IdM, e.g. (replace CONN)
#   nmcli connection modify CONN ipv4.dns 192.168.122.4
#   nmcli connection modify CONN ipv4.dns-search '~laptop.lab'
#   nmcli connection up CONN
# - libvirt default network dnsmasq: in /var/lib/libvirt/dnsmasq/*.conf, forward the zone, then
#   virsh net-destroy default && virsh net-start default (or restart libvirtd):
#   server=/laptop.lab/192.168.122.4

debian enablement
- satellite-maintain packages install python3.12-pulp-deb
- satellite-installer --katello-enable-deb=true
